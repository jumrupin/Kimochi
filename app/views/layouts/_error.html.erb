<% if user.errors.any? %>
  <div class="alert alert-warning">
    <ul>
      <% user.errors.full_messages.each do |message| %>
        <h4>error<h4>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>
